---
options:
  subPackages:
    - name: "service"
    - name: "topic"

packages:
  - name: j5.registry.registry.v1
  - name: j5.registry.github.v1

registry:
  owner: pentops
  name: registry

dependencies:
  - registry:
      owner: pentops
      name: messaging

