---
options:
  subPackages:
    - name: "service"
    - name: "topic"

packages:
  - name: j5.registry.registry.v1
  - name: j5.registry.github.v1

registry:
  owner: pentops
  name: registry

dependencies:
  - bufRegistry:
      owner: bufbuild
      name: protovalidate

  - bufRegistry:
      owner: googleapis
      name: googleapis

  - registry:
      owner: pentops
      name: j5

  - registry:
      owner: pentops
      name: messaging

